env: python
envVersion: 3.11.1  # 👈 must be at the top level, not inside 'services'

services:
  - type: web
    name: rice-prediction-app
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: |
      gunicorn rice_prediction.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        value: postgresql://r_dot_db_user:XxHqnxouFEGHMspbwBGzi0BpPizIdm9x@dpg-d3def6ripnbc73c9iflg-a/r_dot_db
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "rice-prediction-web.onrender.com"
    autoDeploy: true

  - type: pserv
    name: rice-prediction-db
    runtime: postgres
